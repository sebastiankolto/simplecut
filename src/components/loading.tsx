import React from 'react';

const Loading: React.FC = () => {
  //   TODO: Finish a loader for when the user changes language
  return (
    <div className="w-screen h-screen border-2 border-primary-500">
      <h1>Loading text</h1>
    </div>
  );
};

export default Loading;
